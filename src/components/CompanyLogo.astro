---
interface Props {
  initials: string;
  logo?: string;
  technology?: boolean;
}

const { initials, logo, technology = false } = Astro.props;
---

<span class:list={['company-logo', { 'company-logo--technology': technology }]} aria-hidden="true">
  {logo ? <img src={logo} alt="" loading="lazy" decoding="async" /> : initials}
</span>
